import { createTheme } from "@mui/material/styles";

const tema = createTheme({
  palette: {
    primary: { main: "#9c27b0" },   // mor
    secondary: { main: "#e91e63" }, // pembe
  },
});

export default tema;
